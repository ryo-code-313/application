# きのみタイプ厳選チェッカー

ポケモンスリープのきのみタイプ向けの厳選チェッカーです。きのみタイプの最終進化形（イベント限定の姿を含む31匹）に対応しています。サブスキル・性格から、1日あたりのきのみエナジーの期待値を計算し、無補正個体との比較や全パターン中の順位を表示します。

ミュウツー版（リポジトリ直下）・食材タイプ版（`ingredient/`）と同じ画面構成で、サブスキル・性格のデータ、げんきとおてつだいのタイミング計算（`js/calc.js` の `schedule`）、サブスキル抽選モデル（`subsetDist`）、スタイルを共有しています。

## 構成

- `index.html` — マークアップ
- `css/theme.css` — きのみタイプ版の配色（レイアウトは `../css/style.css`）
- `js/constants.js` — きのみタイプ版だけで使う定義
- `js/mons.js` — ポケモンごとの基礎値
- `js/calc.js` — 期待値計算エンジン（DOM非依存）
- `js/worker.js` — 上位%の分布計算をバックグラウンドで実行するWeb Worker
- `js/state.js` — アプリ状態管理・localStorage永続化（キーは `bf` 接頭辞）
- `js/ui.js` — DOM描画・イベント処理
- `js/main.js` — エントリーポイント

## ポケモンのデータ

`js/mons.js` の `MONS` にポケモンごとの基礎値（おてつだい時間・食材確率・最大所持数・1回あたりのきのみの個数・きのみの基礎エナジー・食材スロットの候補と個数）を持ちます。画面上部でポケモンを選び、`?mon=walrein` のように URL でも指定できます。選んだポケモンは次に開いたときにも使います。

データは [RaenonX ポケスリ攻略ツール](https://pks.raenonx.cc/ja/pokedex) から 2026年9月25日に取得しました。

- 対象は、とくいが「きのみ」で、これ以上進化しないポケモンです。ピカチュウ(ハロウィン) のオレンジとパープルは基礎値が同じなので1つにまとめています。
- 食材確率は RaenonX の集計値です。集計件数が少ないポケモンは誤差が大きい可能性があります。いくつかのポケモンは [ポケモンスリープ個体値チェッカー](https://www.pokemonsleepdaifuku.com/) の参考値と照らし合わせ、差が0.3ポイント以内であることを確かめました。
- きのみのエナジーは、RaenonX の Lv.1〜100 の値が下の計算式と一致することを全ポケモンで確かめました。
- 最大所持数は、その姿で捕まえた個体の値です。進化で増える分は考慮していません。

| ポケモン | きのみ（Lv.1 エナジー） | おてつだい時間 | 食材確率（集計件数） | 最大所持数 |
|---|---|---|---|---|
| バタフリー | ラムのみ（24） | 2500秒 | 19.7%（48件） | 21 |
| ラッタ | キーのみ（28） | 2950秒 | 23.7%（21件） | 16 |
| アーボック | カゴのみ（32） | 3400秒 | 26.4%（18件） | 14 |
| ライチュウ | ウブのみ（25） | 2200秒 | 22.4%（245件） | 21 |
| ピクシー | モモンのみ（26） | 2800秒 | 16.8%（10件） | 24 |
| キュウコン | ヒメリのみ（27） | 2600秒 | 16.4%（9件） | 23 |
| オコリザル | クラボのみ（27） | 2800秒 | 20.0%（120件） | 17 |
| ドードリオ | シーヤのみ（24） | 2300秒 | 18.4%（13件） | 21 |
| ガラガラ | フィラのみ（29） | 3300秒 | 22.5%（19件） | 15 |
| メガニウム | ドリのみ（30） | 2800秒 | 17.5%（92件） | 20 |
| バクフーン | ヒメリのみ（27） | 2400秒 | 20.8%（73件） | 23 |
| オーダイル | オレンのみ（31） | 2800秒 | 25.7%（111件） | 19 |
| ネイティオ | マゴのみ（26） | 2500秒 | 19.1%（17件） | 19 |
| ハガネール | ベリブのみ（33） | 3000秒 | 15.4%（34件） | 25 |
| ヘルガー | ウイのみ（31） | 3300秒 | 20.3%（30件） | 16 |
| バシャーモ | クラボのみ（27） | 2600秒 | 15.3%（10件） | 22 |
| ラグラージ | フィラのみ（29） | 2800秒 | 14.6%（13件） | 20 |
| ケッキング | キーのみ（28） | 3600秒 | 33.9%（10件） | 16 |
| チルタリス | ヤチェのみ（35） | 3500秒 | 25.8%（13件） | 14 |
| ジュペッタ | ブリーのみ（26） | 2600秒 | 17.9%（60件） | 19 |
| トドゼルガ | チーゴのみ（32） | 3000秒 | 22.3%（71件） | 18 |
| ボーマンダ | ヤチェのみ（35） | 2800秒 | 21.7%（7件） | 22 |
| エンペルト | ベリブのみ（33） | 3200秒 | 16.8%（15件） | 18 |
| マニューラ | ウイのみ（31） | 2700秒 | 25.1%（18件） | 21 |
| ムシャーナ | マゴのみ（26） | 2800秒 | 18.8%（25件） | 24 |
| ガチゴラス | オボンのみ（30） | 2800秒 | 17.8%（10件） | 23 |
| デカヌチャン | モモンのみ（26） | 2400秒 | 18.5%（10件） | 20 |
| キュウコン(アローラのすがた) | チーゴのみ（32） | 2900秒 | 23.2%（13件） | 20 |
| ピカチュウ(ハロウィン) | ウブのみ（25） | 2500秒 | 21.8%（7件） | 18 |
| イーブイ(ホリデー) | キーのみ（28） | 3100秒 | 15.6%（8件） | 20 |
| ピカチュウ(キャプテン) | ウブのみ（25） | 2500秒 | 17.5%（10件） | 21 |

## 計算モデル

「きのみの1日の期待エナジー」を、無補正個体（サブスキルなし・無補正性格）の値で割った比を順位の基準にしています。

- おてつだい時間・げんき・いいキャンプチケット・日をまたぐ持ち越しはミュウツー版と同じ（リポジトリ直下の README.md を参照）。
- きのみ1個のエナジー = max(基礎 + Lv − 1, round(基礎 × 1.025^(Lv − 1)))。Lv.60（3枠）/ Lv.70（4枠）で計算する。
- きのみおてつだい1回で拾うきのみは、きのみタイプの2個（きのみの数Sで3個）。
- 起床時に所持品を受け取り、日中はタップしない（いつのまに育成）。起床から次の起床までを所持数0から続けて追う。
- 満タンになるまでは、食材確率 = 基礎値 × 性格(↑1.2 / ↓0.8) × (1 + 食材確率アップ合計) で食材おてつだいになり、それ以外はきのみおてつだい。食材おてつだいでは食材配列の3スロット（Lv.1 / Lv.30 / Lv.60）から均等に1つ選び、そのスロットの個数を拾う。
- 満タンになった後は、食材確率に関係なくすべてのおてつだいできのみ（きのみの数Sの効果込み）を拾い、所持数からあふれたきのみはエナジーになる（いつのまに育成）。満タンになったおてつだいであふれた食材は捨てられる。おてつだいキューに残った最大4回分はメインスキルの抽選にだけ使われ、拾うものはきのみになる。
- そのため、1日のきのみ = 1回あたりのきのみ × (おてつだい回数 − 満タンになる前の食材おてつだい回数)。食材確率は満タンになるまでの数回にしか効かず、所持数を増やすと満タンが遅れて食材おてつだいが増えるぶん、きのみのエナジーはわずかに減る。
- 食材配列は入力しない。各スロットの候補を等確率として、配列ごとの値を出現確率で平均する（たとえばトドゼルガは Lv.60 の候補がすべて4個なので、Lv.30 がオイル×2 かマメミート×3 かだけが効く）。
- 性格のスキル補正はきのみに影響しないので「なし他」として扱う。
- 上位%の分布は、サブスキル（色別抽選・重複なし）と性格25種をミュウツー版と同じモデルで数え上げる。

### 考慮していないもの

- 食材とメインスキルによるエナジー（きのみのエナジーのみで比較）。満タンの間はスキルも抽選されない。
- 日曜夜から月曜朝にかけては満タン後にいつのまに育成へ移行しない（週の最終日だけの例外なので、毎日同じとして計算）。
- イベントボーナスの「通常のおてつだいで拾うきのみ+1」（いつのまに育成では効かない）。
- おてつだいボーナスのチーム全体への効果（自分のおてつだい時間の短縮としてのみ計算）。
- 性格のげんき回復量補正（ミュウツー版・食材タイプ版と同じく、起床時のげんきは補正なしで計算）。

### 出典

- いつのまに育成・おてつだいキュー: [ポケモンスリープ攻略・検証 Wiki](https://wikiwiki.jp/poke_sleep/)（満タン後は食材確率0%・きのみ確率100%になり、きのみの数Sは効く。キューの残りはスキル抽選のみ）、[ゲームエイト](https://game8.jp/pokemonsleep/584756)
- きのみのエナジー: [RaenonX ポケスリ攻略ツール](https://pks.raenonx.cc/ja/pokedex/365) の Lv.1〜100 の値が上の式と一致することを確認

## ローカルで動かす

リポジトリ直下で簡易サーバーを起動し、`http://localhost:8000/berry/` を開いてください。

```bash
python3 -m http.server 8000
```
